# Railway Deployment Configuration
# Add these environment variables in Railway dashboard: Project → Variables

# Google Gemini API Key (REQUIRED - Get free from https://aistudio.google.com/app/apikey)
GOOGLE_API_KEY=your_api_key_here

# Flask Configuration for Production
FLASK_DEBUG=False
FLASK_PORT=5000

# Database Configuration (SQLite works, but can upgrade to PostgreSQL on Railway)
DATABASE_URL=sqlite:///resume_matcher.db

# Railway will automatically set PORT env variable, Procfile uses it
# No need to set PORT here - Railway handles it
